# PyInstaller build artifacts
build/
dist/
*.spec
!HushSnap.spec
__pycache__/

# Test-only native crash trigger (built locally by native/build_crashlib.bat;
# never shipped, never committed). Only assets/crashlib.dll is bundled by the
# .spec when present; the rest are MSVC link artifacts.
assets/crashlib.*
vc140.pdb

# Installer build artifacts
dist-installer/
dist-installer-dev/
dist-installer-test/

# Executables
*.exe

# IDE / Editor
.vscode/
.vs/
.claude/
.antigravitycli/

# Tool output / profiling data
scratch/

# Stress-test generated artifacts (logs, OCR result variants)
stress_results/
ocr_stability_results/

# Test cache
.pytest_cache/

# Orphaned build links
*.lnk


