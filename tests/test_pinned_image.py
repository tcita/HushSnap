# PinnedImageWindow tests.
# Visual behaviors (sizing, resize, aspect-ratio) are verified manually
# because they depend on DPR, shadow padding, and native window geometry
# that are painful to mock reliably in a headless test.
